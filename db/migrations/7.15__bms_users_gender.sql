-- =============================================================
-- 7.15  users.gender — เพศของแอดมิน (ใช้เลือกคำลงท้าย ครับ/ค่ะ ใน AI แนะนำคำตอบ)
-- -------------------------------------------------------------
-- เดิมไม่มี field เพศเลย · 'male' = ลงท้าย "ครับ", 'female' = "ค่ะ" (ค่าเดิม),
-- NULL = ไม่ระบุ → คงคำลงท้ายหญิงเดิมไว้ (ตาม default ปัจจุบันของระบบ)
-- ไม่บังคับค่า (validate ที่ app layer — updateMe รับเฉพาะ 'male'/'female'/null)
-- =============================================================

ALTER TABLE users ADD COLUMN IF NOT EXISTS gender TEXT;
